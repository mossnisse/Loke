package main;

/* a block comment /* that nests */ and closes here */
main :: proc() {
	mask := 0b1111 &~ 0x3;      // longest match: `&~`, not `&` then `~`
	shifted := 0o10 << 2;       // 8 << 2
	print_int(mask);
	print_int(shifted);
	print_int(1_000 - 968);     // `_` separators
}
