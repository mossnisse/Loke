package main; import "core:fmt";

// The selected allocator factory answers `nil`, which terminates startup before
// this runs. Reaching `main` at all is the failure.

main :: proc() {
	fmt.println("startup should not have reached main");
}
