package main; import "core:fmt";

main :: proc() {
	// A failed single-value checked extraction from an `any_view` traps.
	view: any_view = 7;
	if (view.(bool)) { fmt.println(1); } else { fmt.println(0); }
}
