package main; import "core:fmt";

Res :: struct { id: int }

impl Res {
	drop :: proc(self: inout Res) { fmt.println(self.id); }
}

fail :: proc() -> int {
	panic("later map value failed");
	return 0;
}

main :: proc() {
	m: map[int]Res = {1 = Res{1}, 2 = Res{fail()}};
}
