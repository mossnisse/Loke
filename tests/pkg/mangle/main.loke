package main; import "core:fmt";

// These paths used to collapse to the same LLVM package prefix.
import dash "a-b";
import dot "a.b";

main :: proc() {
	fmt.println(dash.value());
	fmt.println(dot.value());
}
