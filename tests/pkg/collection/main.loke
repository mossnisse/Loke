package main;

// A collection prefix resolves only through `-collection`; there is no
// built-in `core:` root.
import "lib:strings";
import alias "lib:strings";

main :: proc() {
	print_int(strings.width());
	print_int(alias.width());
}
