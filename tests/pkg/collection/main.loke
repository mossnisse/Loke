package main; import "core:fmt";

// A collection prefix resolves only through `-collection`; there is no
// built-in `core:` root.
import "lib:strings";
import alias "lib:strings";

main :: proc() {
	fmt.println(strings.width());
	fmt.println(alias.width());
}
