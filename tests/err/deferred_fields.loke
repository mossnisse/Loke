package main;

// Several deferred field types, one diagnostic: the gate is per declaration,
// not per field.
Record :: struct {
	name:  string,
	other: string,
	tag:   []u8,
}

main :: proc() {
	print_int(1);
}
