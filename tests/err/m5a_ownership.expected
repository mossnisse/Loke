L0502
@29:9
`Only` is move-only, so a borrowed value of it cannot be returned by value; return a `move` parameter or a local instead
L0499
@35:7
`drop` borrows `value`, so this body does not own it; `move` at the call site is what transfers ownership
L0500
@43:14
`a` cannot be used here: it has already been moved, dropped, or released
L0500
@47:2
`c` cannot be dropped here: it has already been moved, dropped, or released
L0497
@55:7
`drop` names a variable; move the owner out of the aggregate first, or drop the whole value
L0498
@58:7
`drop` cannot be applied to file-scope storage, which is always live
L0501
@64:19
`value` is a `move` parameter, so this argument is written `move(...)`
L0503
@68:7
`Only` is move-only, so this binding cannot copy it; write `move(...)` to transfer ownership instead
L0503
@69:6
`Only` is move-only, so this assignment cannot copy it; write `move(...)` to transfer ownership instead
L0491
@75:19
`Only` is move-only, so a by-value `foreach` cannot copy it out of the container; iterate `source.refs()` for read-only access, `&value` for mutable access, or remove the elements
L0491
@79:19
`Only` is move-only, so this view cannot copy the value out of the map
L0491
@82:20
`Only` is move-only, so this view cannot copy the value out of the map
L0505
@89:16
`exchange` replaces the destination, so it is written `exchange(inout place, replacement)`
L0500
@91:7
`h` cannot be exchanged here: it has already been moved, dropped, or released
L0498
@97:7
`drop` cannot be applied to `static` storage, which is always live
L0498
@98:12
`move` cannot be applied to `thread_local` storage, which is always live
L0506
@99:18
a `static` declaration is initialised once, before any code runs, so its initialiser must be a compile-time constant
L0500
@106:14
`d` cannot be used here: it is live on only some of the paths that reach this point
L0501
@111:23
`value` is a `move` parameter, so this argument is written `move(...)`
