package main; import "core:fmt";

// A by-value cycle has no finite size; the pointer-recursive `Ok` does.
A :: struct { b: B }
B :: struct { a: A }

Ok :: struct { next: ^Ok }

main :: proc() {
	fmt.println(1);
}
