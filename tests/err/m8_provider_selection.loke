package main;

import "core:log";

// Command-line provider-selection diagnostics. The selections deliberately
// remain in the sibling `.flags` to cover build overrides, which have no source
// location to point at.

main :: proc() {
	log.error("unreached");
}
