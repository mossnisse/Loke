package main; import "core:fmt";

stray :: proc() {
	break;
	continue;
}

escaping :: proc() -> int {
	defer return 1;
	for (i := 0; i < 2; i += 1) {
		defer break;
	}
	defer {
		defer fmt.println(1);
	}
	return 0;
}

main :: proc() {
	stray();
	fmt.println(escaping());
}
