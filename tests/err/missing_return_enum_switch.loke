package main; import "core:fmt";

Foo :: enum { A, B, C }

// Coverage alone does not terminate control flow: one case completes normally.
classify :: proc(f: Foo) -> int {
	switch (f) {
	case .A: return 1;
	case .B: break;
	case .C: return 3;
	}
}

main :: proc() {
	fmt.println(classify(.B));
}
